# Snapmaker U1 safety gates

- Default upload path is `print=false` / upload-only.
- Starting requires idle printer state, expected tool/material, successful upload, fresh LED-on bed photo, and explicit operator `Start print` selection.
- Bed-clear prompt defaults to Cancel.
- Unknown or stale evidence means stop.
