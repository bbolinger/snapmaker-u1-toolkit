# Snapmaker U1 10-step question flow

The toolkit workflow asks instead of guessing: orientation, loaded tool/material, profile, render review, support choice, slice, preview, upload mode, camera snapshot, bed-clear/start confirmation. Upload-only is the default; final bed-clear defaults to Cancel.
